/*
    JqueryUI en JavaScript | Plugin para crear interfaces con jquery
*/

'use strict'

$(document).ready(function (){

    // Date picker - Calendario
    // Un tipico calendario en el que seleccionas una fecha y esta la pone en el camppo que ibas a llenar
    $('[name="calendar"]').datepicker();// Este metodo se le aplica a un input y listo
});
